<UserControl x:Class="ToggleTestApp.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:easyWindowsTerminalControl="clr-namespace:EasyWindowsTerminalControl;assembly=EasyWindowsTerminalControl"
             Background="#1E1E1E">
    <Grid>
	    <Grid>
		    <easyWindowsTerminalControl:EasyTerminalControl StartupCommandLine="powershell" FontFamilyWhenSettingTheme="Consolas" LogConPTYOutput="True"  Win32InputMode="True" InputCapture="TabKey,DirectionKeys" >
			    <easyWindowsTerminalControl:EasyTerminalControl.Resources>
				    <Style TargetType="ScrollBar" BasedOn="{StaticResource {x:Type ScrollBar}}">
					    <!--Lazy Theming-->
					    <Setter Property="Opacity" Value="0.7"/>
				    </Style>
			    </easyWindowsTerminalControl:EasyTerminalControl.Resources>
		    </easyWindowsTerminalControl:EasyTerminalControl>
	    </Grid>
    </Grid>
</UserControl>
