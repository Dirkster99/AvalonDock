<UserControl x:Class="ToggleTestApp.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppEditorBg}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      x:Name="OutputScroller">
            <TextBlock Text="{Binding Output}" FontFamily="Cascadia Mono,Consolas,Courier New"
                       FontSize="12" Foreground="{DynamicResource AppText}" Padding="8"
                       TextWrapping="Wrap" />
        </ScrollViewer>

        <Grid Grid.Row="1" Background="{DynamicResource AppInputBarBg}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="❯" Foreground="#007ACC"
                       FontFamily="Cascadia Mono,Consolas" FontSize="13"
                       Margin="8,4" VerticalAlignment="Center" />

            <TextBox Grid.Column="1" Text="{Binding InputCommand, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Cascadia Mono,Consolas,Courier New" FontSize="12"
                     Background="Transparent" Foreground="{DynamicResource AppText}" BorderThickness="0"
                     CaretBrush="{DynamicResource AppText}" Padding="0,4"
                     x:Name="InputBox" />
        </Grid>
    </Grid>
</UserControl>
